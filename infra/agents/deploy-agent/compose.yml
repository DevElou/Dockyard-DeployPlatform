services:
  deploy-agent:
    build:
      context: ../../..
      dockerfile: build/deploy-agent.Dockerfile
    image: dockyard/deploy-agent:latest
    container_name: dockyard-deploy-agent
    restart: unless-stopped
    environment:
      DOCKYARD_AGENT_ADDR: ":8090"
      DOCKYARD_AGENT_KEY: ${DOCKYARD_AGENT_KEY:?DOCKYARD_AGENT_KEY is required}
    ports:
      - "8090:8090"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - dockyard_platform

networks:
  dockyard_platform:
    external: true
