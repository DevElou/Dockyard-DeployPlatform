services:
  redis:
    image: redis:7.4-alpine
    container_name: dockyard-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - ${REDIS_DATA_DIR:-/opt/dockyard/redis}:/data
